/* ============================================================
   Shared bootstrap for individual portfolio pages
   ============================================================ */
import { mountContactButtons } from "./contact.js";

mountContactButtons();
